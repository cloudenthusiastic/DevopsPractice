@Library("shared-library") _

pipeline {
    agent any
    stages{
        stage ('Checkout'){
            steps{
                script{
		 helloWorld()
                }
            }
        }
    }
}
